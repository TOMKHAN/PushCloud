<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="hr.foi.tosulc.MainActivity" >

    <TextView
        android:id="@+id/pushCloudAppName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:text="@string/app_name"
        android:textSize="25sp" />

    <ImageButton
        android:id="@+id/btnListaSponzora"
        android:layout_width="170dp"
        android:layout_height="120dp"
        android:layout_centerInParent="true"
        android:background="@drawable/selector_sponsor_activity"
        android:contentDescription="@string/app_name" />

    <ImageButton
        android:id="@+id/btnListaPoruka"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:background="@drawable/selector_message_list"
        android:contentDescription="@string/app_name" />

</RelativeLayout>